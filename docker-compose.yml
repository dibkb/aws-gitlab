version: "3.8"
services:
  api:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
    command: poetry run uvicorn app.main:app --host 0.0.0.0 --port 8090 --reload
